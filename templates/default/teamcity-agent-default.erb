# This script is sourced into teamcity-agent startup script
# Automatically generated by chef for <%= node[:fqdn] -%>

# TODO this script should be managed by teamcity::agent recipe, currently
# is managed by teamcity::ruby recipe

# Source rbenv file if present in the system
[ -f /etc/profile.d/rbenv.sh ] && . /etc/profile.d/rbenv.sh
